<h1 style="padding: 40px;">Let Us Know a Little Bit More, {{userResults.first_name}}! </h1>
<h2>Please Complete Your Profile Below</h2>
<div class="card updateCard">
    <h4>Personal Info</h4>
    <form class="form personalInfo">
        <div class="form-group">
            <label for="first_name">First Name:</label>
            <input type="text" name="firstname" placeholder="{{userResults.first_name}}" id="first_name" readonly>
        </div>
        <div class="form-group">
            <label for="last_name">Last Name:</label>
            <input type="text" name="lastname" placeholder="{{userResults.last_name}}" id="last_name" readonly>
        </div>
        <div class="form-group">
            <label for="email"> Email:</label>
            <input type="text" name="email" placeholder="{{userResults.email}}" id="email" readonly>
        </div>
        <div class="form-group">
            <label for="age">Age:</label>
            <input type="number" name="age" value="{{userResults.age}}" id="age" placeholder="Please Enter your age">
        </div>
        <div class="form-group" name="sex">
            <label for="sex">Sex:</label>
            <select class="form-control" id="sex">
                <option value="male">Male</option>
                <option value="female">Female</option>
            </select>
        </div>
        <div class="form-group">
            <label for="weight">Weight:</label>
            <input type="number" name="weight" value="{{userResults.weight}}" id="weight" placeholder="in lbs">
        </div>
        <div class="form-group">
            <label for="height">Height:</label>
            <input type="number" name="height" value="{{userResults.height}}" id="height" placeholder="in inches">
        </div>
        <div class="form-group">
            <label for="goal">Your Target Goal:</label>
            <input type="number" name="goal" value="{{userResults.goal}}" id="goal" placeholder="in lbs">
        </div>
        <div class="form-group" name="activity_level">
            <label for="activity_level">Activity Level:</label>
            <select class="form-control" id="activity_level">
                <option value="sedentary">Sedentary</option>
                <option value="lightly active">Lightly Active</option>
                <option value="moderately active">Moderately Active</option>
                <option value="very active">Very Active</option>
                <option value="extra active">Extra Active</option>
            </select>
        </div>
        <button style="margin: 40px;" class="btn" type="submit">Save Changes</button>
    </form>
</div>

<script src="./js/update-dashboard.js"></script>
